import styled from 'styled-components';

const FavoritesStyle = styled.div`
  .favorites-title {
    margin-bottom: 60px;

    h1 {
      font-size: 20px;
      line-height: 34px;
    }
  }
`;

export default FavoritesStyle;
